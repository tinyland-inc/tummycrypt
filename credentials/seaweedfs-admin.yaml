# SeaweedFS admin credentials
# This file should be SOPS-encrypted before committing.
# Run: scripts/sops-init.sh && scripts/migrate-credentials.sh
#
# After encryption, this file will look like:
#   region: us-east-1      (plaintext, safe)
#   jwt_signing_key: ENC[AES256_GCM,...]   (encrypted)
#   sops: { age: [...] }
#
# To decrypt: sops decrypt credentials/seaweedfs-admin.yaml
# To use in shell: sops exec-env credentials/seaweedfs-admin.yaml 'your-command'
#
# PLACEHOLDER - run scripts/migrate-credentials.sh to populate and encrypt
region: us-east-1
endpoint: http://dees-appu-bearts:8333
filer_endpoint: http://192.168.101.146:8888
jwt_signing_key: SOPS_MIGRATE_REQUIRED
rclone_password1: SOPS_MIGRATE_REQUIRED
rclone_password2: SOPS_MIGRATE_REQUIRED
